"""Out-of-band operator tools, never imported by ``python -m worker``.

``deepstream_canary`` is host-side and excluded from the image;
``fetch_models`` ships in the image as the ``edge-model-fetch`` one-shot.
"""
