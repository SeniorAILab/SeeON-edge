# Workspace member: shared library (shared.events). Pure stdlib + contracts (path).
[project]
name = "eldercare-shared"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
