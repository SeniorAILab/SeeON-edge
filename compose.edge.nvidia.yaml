# NVIDIA infrastructure overlay. Required for the unified
# ML_WORKER_PROFILE=nvidia profile.
services:
  ml-worker:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    environment:
      NVIDIA_DRIVER_CAPABILITIES: compute,utility,video
